apiVersion: v1
kind: Secret
metadata:
  name: tls-secret
  namespace: fluxforge
type: Opaque
stringData:
  # In production, use valid CA-signed certificates
  # To generate: openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout certs/fluxforge.key -out certs/fluxforge.crt ...
  # Then base64 encode contents or use stringData
  # fluxforge.crt: |
  #   -----BEGIN CERTIFICATE-----
  #   ...
  #   -----END CERTIFICATE-----
  # fluxforge.key: |
  #   -----BEGIN PRIVATE KEY-----
  #   ...
  #   -----END PRIVATE KEY-----
  
  # Placeholder for demonstration (replace with actual cert content)
  fluxforge.crt: "PLACEHOLDER_CERTIFICATE"
  fluxforge.key: "PLACEHOLDER_KEY"
